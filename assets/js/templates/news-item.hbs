{{#each news}}
	<article class="col">
		<div class="photo-holder">
			<figure>
				<img src="build/images/news/{{ photo }}" alt="{{ title }}">
			</figure>
			<time>{{ date }}</time>
		</div>
		<h2>{{ title }}</h2>
		<p>{{ text }}</p>
	</article>
{{/each}}